# Singularity turbo sample

Process 2 of `gemmy video h3 generate --dit singularity`. Full INT8 UNET, turbo LoRA only, scheduler simple/6, extend 2, split at 2, 1.25x latent upscale. No CLIP.

Not published.
