# comfy-workflow-h3-ganloss-stage1-still-audio

Eros still-only stage 1 with one live `--ref-audio`. Same SplitSigmas@4 graph as stage1-still plus `LoadAudio` on `ref_audios.ref_audio_0`.

AUTHORING SOURCE: `ir.build.ts`. Rebuild with `pnpm comfy:build`.
Unofficial project. Not affiliated with or endorsed by Comfy Org or MiniMax.
