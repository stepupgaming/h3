This installs h3 for the current Windows user.

The setup then creates the Python environment, installs ffmpeg if it is missing, downloads the product Comfy node packs, and downloads the model weights those commands need. That download is large and shows its progress in a console window.
