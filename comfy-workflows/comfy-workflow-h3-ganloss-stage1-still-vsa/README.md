# Eros still-only stage 1 + VSA

Local Gemmy variant. Same SplitSigmas@4 graph as stage1-still with `Ref2VAVSAGatePatch` on MODEL. Runtime binds `gate_file` and `sparsity`. Do not edit generated JSON.
