h3 is installed. Open a new terminal so PATH picks it up, then:

  h3 setup
  h3 download base
  h3 download eros
  h3 doctor
  h3 generate --first-frame still.png --prompt "..." --duration 5 -o clip.mp4

uv and ffmpeg are separate installs. Model weights are fetched by h3 download.
