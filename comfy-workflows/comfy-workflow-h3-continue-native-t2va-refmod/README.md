# H3 Native-Guide Continue + RefMod (Text Encode)

Continue from a persisted AV latent while injecting saved RefMods through Text Encode (`<Picture n>` / `<Video n>`). No live still. Default DiT is Ref2VA.

AUTHORING SOURCE: `ir.build.ts`. Rebuild: `pnpm comfy:build`.
