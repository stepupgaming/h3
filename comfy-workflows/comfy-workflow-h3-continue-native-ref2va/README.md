# H3 Native-Guide Continue (Ref2VA still)

Continue from a persisted AV latent with one live `--ref-image`. The previous tail is `minimax_keyframes`; the still is `minimax_refs`. Runtime binds Eros or stock Ref2VA via `unet`.

AUTHORING SOURCE: `ir.build.ts`. Rebuild: `pnpm comfy:build`.
